Django>=5.0,<6.0
djangorestframework>=3.15
django-cors-headers>=4.4
requests>=2.32
python-decouple>=3.8
dj-database-url>=2.2
gunicorn>=22.0
# Serves Django admin static files (CSS/JS) in production (DEBUG=False) so the
# /admin dashboard is styled without a separate static file server.
whitenoise>=6.7
# Postgres driver — only used when DATABASE_URL points at Postgres (e.g. the
# docker-compose `db` service). SQLite remains the zero-config local default.
psycopg[binary]>=3.2
# AWS SDK — minting presigned URLs for private-S3 LUT file delivery (real mode).
boto3>=1.34
# Stripe — hosted Checkout payments (active when STRIPE_SECRET_KEY is set).
stripe>=9.0
# Redis client — backs shared DRF rate-limit counters (active when REDIS_URL set).
redis>=5.0
# Error monitoring — initialized only when SENTRY_DSN is set (see settings).
sentry-sdk[django]>=2.0
# Apple Sign In identity-token verification (only used when APPLE_CLIENT_ID set).
pyjwt[crypto]>=2.8

# Test dependencies
pytest>=8.0
pytest-django>=4.8
