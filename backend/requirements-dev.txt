# Development / test-only dependencies (not needed at runtime).
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# Mock AWS for S3 presigned-URL tests (guarded import in tests).
moto[s3]>=5.0

# Load / bottleneck testing. Run: locust -f backend/loadtest/locustfile.py
locust>=2.20
